package norm.syntaxer;

public enum SyntaxerState {
                IF,
                THEN,
                ELSE,
                WHILE,
                READ,
                WRITE,
                INT,
                INTM,
                DO,
                DEFAULT
}
